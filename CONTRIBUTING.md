# VoxInk 协作规则

本文是 VoxInk 项目协作规则的唯一入口，适用于 issue、提交、Pull Request（PR）和 release。开始工作前，请先阅读本文及相关正式文档。

## 1. 文档权威与设计变更

1. `docs/` 直属的正式文档是产品、架构、供应商和 Windows 输入决策的权威来源，优先级高于 issue 中的执行描述。
2. `docs/research/` 只保存可追溯的研究证据，不代表产品承诺，也不能替代正式文档。
3. Issue 用于把已确定的要求拆成可执行工作，不能静默新增、覆盖或改变设计。
4. 如果实现需要改变设计，先停止实现并更新相应正式文档或决策记录，完成评审后再同步更新 issue。不得通过代码、issue 或 PR 描述绕过这一顺序。
5. README 中描述的能力在代码实现并经验证前都只是目标；不得把脚手架状态表述为可用产品。

## 2. Issue-first 工作流

功能、修复和研究工作原则上先创建 issue，再开始实现。Issue 是执行边界，不是正式设计的替代品。

每个 issue 必须对应一个可独立验收的交付，并包含：

- **目标与交付物**：说明完成后会得到什么，以及由谁或用什么方式验收。
- **依赖**：列出前置 issue、正式文档、外部决策或环境；没有依赖时明确写“无”。
- **范围与非目标**：同时写清要做和刻意不做的内容。
- **实现或研究约束**：给出执行所需的既有决策、接口边界、平台限制、来源要求等，内容应足以让执行者完成工作而无需重新设计。
- **验收标准**：使用可观察、可判定的结果，避免“基本可用”“适当处理”等模糊表述。
- **验证与证据**：预先说明要执行的自动化命令和测试，以及该交付确实需要的平台/真实账户检查；同时说明关闭 issue 时要附上的原始输出、日志、截图或链接。证据不得包含凭据或敏感信息。
- **风险与回滚点**：列出已知风险、未覆盖场景和可逆操作；没有已知风险时明确写“无已知风险”。

拆分时优先缩小交付范围，不要把多个只能分别验收的功能、修复或研究问题放进同一 issue。依赖其他工作的 issue 必须使用 GitHub 可识别引用（如 `#123` 或完整 URL）。

如果实现已经存在但缺少 issue，可以补建追踪 issue；issue 应如实标记这是补充追踪，并在关闭时附上对应 commit、PR（如有）和测试/验证证据，不能伪装成事前规划。

## 3. 实现与提交

- 严格遵守 issue 的范围、非目标和正式文档；发现冲突时以正式文档为准并回到 issue 澄清。
- 按可独立理解的功能拆分提交，不混入格式化、重构或其他无关改动。
- Commit message 使用项目工具认可的 **emoji + Conventional Commits** 格式；类型、emoji 和最终格式以项目当前提交消息验证工具的结果为准。
- Commit body 或 footer 应使用 GitHub 可识别引用关联 issue，例如 `Refs: #123`；仅在该提交确实完成对应工作时使用 `Closes #123`、`Fixes #123` 等关闭关键字。
- 提交前检查 diff、状态和验证结果，只暂存当前交付所需文件。
- 不提交凭据、令牌、私钥、真实账户数据、含敏感信息的日志或本地运行数据。

## 4. 推送与 Pull Request

- 推送前确认目标远端、分支和权限；正常推送不得改写共享历史。任何高风险历史改写都不属于默认流程，也不得假设会自动执行。
- PR 必须引用对应 issue，并说明变更、非目标、验证证据、风险、文档更新和敏感信息检查。
- PR 中使用 `Closes #123`、`Fixes #123` 等 GitHub 可识别语法建立可追踪关系；跨仓库时使用完整 URL 或平台可识别的跨仓库引用。
- PR 合并前，仓库要求的 Linux 自动化以及 Windows 2025 `CGO_ENABLED=1` test、vet、build 必须通过；不得以本地结果或 `CGO_ENABLED=0` 编译替代这些检查。
- 验证证据应能由评审者复核。平台能力、第三方服务或真实账户行为无法由普通自动化覆盖时，仅在 issue 验收、prerelease 说明或项目所有者定义的 stable release 验收要求它们；不把真机或真实账户验证设为每个 PR 的固定合并门禁。需要执行时，必须记录验证环境、结果和脱敏证据。
- 如果 PR 暴露出新的设计选择，先更新正式文档/决策和 issue，再继续合并；不能只在 PR 评论中形成隐性设计。

## 5. Issue 与 PR 的关闭证据

关闭 issue 前，应确保：

1. 验收标准逐项有结论；
2. 对应 commit 和 PR 使用 GitHub 可识别引用；
3. 自动化命令及结果已记录；
4. issue 验收明确要求的平台或真实账户验证已完成并提供脱敏证据，或未执行项及其原因已如实记录并转入后续验收；
5. 未完成项、已知限制和后续 issue 已明确记录。

没有 PR 的直接提交也必须在 issue 中附上 commit 引用和验证证据。验证失败或尚未执行时不得写成“已通过”。

## 6. 发布规则

CI artifact 只用于验证自动化构建结果：它是未签名、短期保留的输出，不等于 prerelease 或正式 release，也不得由 workflow 自动发布或签名。

满足以下条件时，可以创建明确标记的 **prerelease**：

- 仓库要求的 Linux 自动化以及 Windows 2025 `CGO_ENABLED=1` test、vet、build 已通过；
- prerelease notes 明确标注构建未签名，并列出缺失能力、已知限制，以及尚未完成的 Windows 真机、第三方服务或真实账户验证；
- 不把脚手架或未验证能力宣称为可用产品，也不把 CI artifact 本身表述为 release。

只有同时满足以下条件，才可以创建 stable GitHub release：

- 对应 milestone 的验收项已全部闭合，并能追溯到 issue、PR/commit 和验证证据；
- 要求的自动化检查全部通过；
- 项目所有者已经定义并确认 stable release 的验收范围；该范围要求的 Windows 真机、第三方服务或真实账户验证已完成并提供脱敏证据；
- VoxInk 自身许可证已经单独决定并落实；许可证未决时不得创建 stable release；
- 已知限制、兼容性边界、升级/回滚注意事项已记录在 release notes 或其引用文档中；
- 发布内容与正式文档一致，不夸大尚未实现或未验证的能力。

当前脚手架不能被宣称为可用产品 release。

## 7. 敏感信息与仓库边界

- 不在 issue、PR、commit、日志、截图或 release notes 中记录凭据及其他敏感信息；证据必须先脱敏。
- 不提交本地运行数据和被 `.gitignore` 排除的敏感文件。
- 项目尚未选择许可证；未经单独决策，不添加 `LICENSE`，也不声明具体许可证。

## 8. 开始与完成检查

开始工作前：确认正式文档、issue 的单一交付、依赖、范围/非目标、约束和验证方法均已明确。

完成工作时：检查 diff 与仓库状态，运行 issue 要求的验证，记录原始结果，更新必要文档，并用 GitHub 可识别引用闭合 issue → commit/PR → 证据链。
